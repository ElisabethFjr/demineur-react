import './Button.scss';

function Button() {
  return (
    <div className="btn-container">
      <button type="button" className="btn">
        Reset
      </button>
    </div>
  );
}

export default Button;
